import React from 'react'

import styles from './MainBody.module.scss'

function MainBody() {
	return (
		<main className={styles.main}>
			<div className={styles.body}>Empty</div>
		</main>
	)
}

export default MainBody
